dotfiles
========

Usage:

```
./dotfile.py

Install dotfiles

optional arguments:
  -h, --help  show this help message and exit
  --mac       Install dotfiles on Mac.
  --linux     Install dotfiles on Linux.
```
